# Username Generator with Express

## Quick Start

After cloning the repo to local machine, open your terminal and do:

```bash
# Go into project folder
cd ./express-name

# Install dependencies
npm install

# Serve on localhost:5000
npm run dev
```
